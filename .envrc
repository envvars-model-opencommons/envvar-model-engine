# .envrc — loads the dev shell and isolated editor from ./dev
#
# The dev environment lives in ./dev, not at the repository root: argenv is
# published to crates.io, and a root flake.nix would be mistaken for Nix
# packaging of the project itself.
#
# Launch the editor with:  code .   (or: code-dev .)
use flake ./dev
